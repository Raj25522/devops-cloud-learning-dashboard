# DevOps & Cloud Learning Dashboard

Modern dashboard built with HTML, CSS and JavaScript.

## Run with Docker

docker build -t devops-dashboard .

docker run -d -p 8080:80 devops-dashboard

## Run with Docker Compose

docker compose up -d
