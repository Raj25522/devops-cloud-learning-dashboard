apiVersion: v1
kind: Service

metadata:
  name: devops-dashboard-service
  namespace: devops-dashboard

spec:
  selector:
    app: devops-dashboard

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort